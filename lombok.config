# Lombok configuration for Eclipse compatibility
# This file must be in the project root directory

# Stop Lombok from looking in parent directories
config.stopBubbling = true

# Add @Generated annotation so Eclipse doesn't flag Lombok code
lombok.addLombokGeneratedAnnotation = true

# Suppress warnings in generated code
lombok.extern.findbugs.addSuppressFBWarnings = false
